package connection;

import commands.*;

public enum CommandResponse {
    HELP,
    INFO,
    SHOW,
    ADD,
    UPDATE,
    REMOVE_BY_ID,
    CLEAR,
    EXECUTE_SCRIPT,
    EXIT,
    HEAD,
    ADD_IF_MIN,
    REMOVE_LOWER,
    SUM_OF_AGE,
    FILTER_CONTAINS_NAME,
    FILTER_STARTS_WITH_NAME,
    REGISTRATION,
    AUTHORIZATION,
    DEFAULT;

}
